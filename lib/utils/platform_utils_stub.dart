void printHtml(String title, String htmlContent) {
  // Esta função não faz nada em plataformas não-web (Android/iOS/Desktop).
  // No futuro, pode-se implementar a geração de PDF aqui.
  // ignore: avoid_print
  print('Impressão não suportada nesta plataforma.');
}
